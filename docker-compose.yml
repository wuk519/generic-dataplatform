services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: dataplatform
      POSTGRES_PASSWORD: dataplatform
      POSTGRES_DB: dataplatform
    ports:
      - "5432:5432"
    volumes:
      - dataplatform_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dataplatform"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  dataplatform_pgdata:
